<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:colorPrimary">@color/mysave</item>
        <item name="android:colorPrimaryDark">@color/mysave</item>
        <item name="android:colorAccent">@color/mysave</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:colorControlActivated">@color/mysave</item>
    </style>

</resources>
